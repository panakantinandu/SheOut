/**
 * Booking module - ride and delivery request lifecycle: creating a booking,
 * fare estimate, cancellation, status (requested, confirmed, in-progress,
 * completed).
 * <p>
 * Only classes declared directly in this package are this module's public
 * API. Everything under {@code com.sheout.booking.internal} is private to
 * this module - other modules must not import from it.
 */
package com.sheout.booking;
