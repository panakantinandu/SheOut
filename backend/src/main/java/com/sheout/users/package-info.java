/**
 * Users module - customer and driver profile data (name, contact info,
 * preferences, account status) that is not itself auth or verification.
 * <p>
 * Only classes declared directly in this package are this module's public
 * API. Everything under {@code com.sheout.users.internal} is private to this
 * module - other modules must not import from it.
 */
package com.sheout.users;
