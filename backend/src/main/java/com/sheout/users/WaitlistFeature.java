package com.sheout.users;

/**
 * Features somebody can ask to hear about before they exist. A fixed list,
 * not a free-text key: the endpoint must not become a place anyone can write
 * arbitrary labels against their account.
 */
public enum WaitlistFeature {
    /** SheOut Seller - women selling products and services through the app. */
    SELLER
}
